name: Deploy to Netlify

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Netlify production build
        run: curl -sS -X POST -d {} "${{ secrets.NETLIFY_BUILD_HOOK }}"
